# ClawSoul 图像人像生成功能 — 可行性分析报告

## 概述

本报告针对在 ClawSoul 中加入"AI 女友自拍"功能进行完整的技术调研和方案设计。涵盖 API/模型选型、一致性方案、集成路径、成本估算、隐私考虑等。

---

## 一、技术方案选型

### 1.1 API 方案（推荐优先）

| 方案 | 模型 | 特点 | 人物一致性 | 成本 | 中文友好度 |
|------|------|------|-----------|------|-----------|
| **OpenAI DALL-E 3** | `dall-e-3` | 质量最高，理解力强，自动优化 prompt | 需额外搭配参照图（无原生 face consistency） | $0.04-$0.08/张 | 一般（英文 prompt 效果更好） |
| **Midjourney API** | MJ v6 / Niji | 艺术风格最佳，二次元/写实都好 | 支持 --cref（角色参照图），一致性最好 | ~$0.01-0.08/张（via 第三方） | 一般 |
| **Stability AI** | SD3.5 / SDXL | 开源模型，ControlNet 支持好 | 可用 IP-Adapter + LoRA 实现强一致性 | $0.004-0.01/张（via API） | 好 |
| **阿里通义万相** | 通义万相 | 中文理解极佳，质量提升快 | 可上传参照图来保证一致性 | ¥0.08-0.16/张 | ★★★★★ |
| **字节跳动 Doubao/星野** | 内部模型 | 二次元/女性角色效果好 | 有角色固定功能 | — | ★★★★★ |
| **腾讯混元** | 混元 DiT | 中文场景优秀，支持局部重绘 | 支持图生图 | ¥0.06/张 | ★★★★★ |
| **Replicate** | SD / Flux / Playground | 统一 API 访问多种模型 | 可用 LoRA + 种子控制 | ~$0.003-0.05/张 | 一般 |
| **Fal.ai** | SD3.5 / Flux Pro | 快速、高并发 | 支持 LoRA 和 Face ID | ~$0.003-0.05/张 | 一般 |

### 1.2 推荐方案

#### 首选方案：通义万相（阿里云）
- **理由**：中文理解极佳，能准确理解"穿白色连衣裙、扎丸子头、在阳台喝咖啡"这类中文描述；质量接近 DALL-E 3；价格适中（¥0.08-0.16/张）；国内访问延迟低。
- **人物一致性**：支持上传参照图（reference image）进行风格迁移，可以作为角色定妆照一致性方案。
- **API 接入**：标准 REST API，Python requests 即可调用。
- **时效性**：每张约 5-15 秒生成。

#### 备选方案：DALL-E 3 + IP-Adapter（组合方案）
- 用 DALL-E 3 生成高质量图片，再通过本地的 IP-Adapter（SDXL）做 face swap 或风格迁移来保证角色一致性。
- 复杂度较高，但质量最高。

#### 高性价比方案：Replicate + Flux Pro + LoRA
- Flux Pro 是目前开源模型中的质量天花板。
- 可训练一个 LoRA 模型来固定角色长相。
- Replicate 提供统一 API，按秒计费。

---

## 二、人物形象一致性方案（最重要的问题）

这是"AI 女友自拍"最核心的技术难点。以下逐个分析方案：

### 方案 A：LoRA 微调（推荐）
- **原理**：用 5-15 张角色定妆照训练一个 LoRA 模型（权重 ~100MB）。
- **效果**：同一角色在不同场景、衣着、角度下长相一致。
- **成本**：
  - 训练：一次约 ¥10-30（Replicate/阿里云），或本地免费（需要 GPU）。
  - 推理：额外带 LoRA 的每次调用成本增加约 0（本地）或 ¥0.01-0.03（云端）。
- **工具**：
  - 本地：`kohya_ss` / `sd-scripts` 训练，SD WebUI / ComfyUI 推理。
  - 云端：Replicate 支持上传 LoRA 权重，`fal.ai` 支持。
  - 阿里云：通义万相暂不支持自定义 LoRA，但支持参照图。
- **适合 ClawSoul 的路径**：初期用参照图方案（通义万相），后期训练 LoRA 提升一致性。

### 方案 B：IP-Adapter Face ID
- **原理**：每次生成时传入一张或多张角色参照图，模型自动提取面部特征。
- **优点**：无需训练，即插即用。
- **缺点**：需要本地或支持此功能的 API 运行 SD 模型。
- **适用场景**：本地跑 SD 时的轻量方案。

### 方案 C：图生图 (img2img) + 种子控制
- **原理**：每次基于同一张"定妆照"做图生图，固定 seed。
- **效果**：一致性较弱（着装、场景改变时容易跑偏）。
- **优点**：最简单，所有 API 都支持。
- **缺点**：角色一致性不够稳定，需要配合强 prompt 描述。

### 方案 D：Midjourney Cref
- **原理**：使用 Midjourney `--cref` 参数传入角色图片 URL。
- **效果**：非常好的一致性，支持 face focus。
- **缺点**：需要 Midjourney 订阅（$10-60/月）或第三方 API（价格不透明）。
- **适合**：高质量、低频使用场景。

### 方案 E：ComfyUI + 工作流复用
- **原理**：在本地搭建 ComfyUI，设计一个包含 FaceID + ControlNet 的工作流，用 API 调用。
- **效果**：可达到最强一致性（FaceID + IP-Adapter + ControlNet 姿态控制）。
- **缺点**：需要 GPU 服务器（至少 8GB VRAM），运维成本高。
- **适合**：对质量要求极高、有 GPU 资源的场景。

### 一致性方案对比总结

| 方案 | 一致性 | 实现难度 | 成本 | 推荐场景 |
|------|--------|---------|------|---------|
| 通义万相参照图 | ★★★☆ | 低 | ¥0.08-0.16/张 | 快速实现、初期阶段 |
| LoRA 训练 | ★★★★★ | 中 | ¥10-30 训练 + 少量推理 | 长期稳定方案 |
| IP-Adapter Face ID | ★★★★ | 中 | 本地 GPU 成本 | 有本地显卡时 |
| Midjourney Cref | ★★★★★ | 低 | $10-60/月 | 高质量低频 |
| DALL-E 3 + 图生图 | ★★☆ | 低 | $0.04-0.08/张 | 最简单的入门 |

---

## 三、本地 vs API 方案对比

### API 方案（推荐）

| 优点 | 缺点 |
|------|------|
| 无需 GPU，任何机器都能跑 | 每张图片需要付费 |
| 生成速度快（5-15秒） | 依赖网络，需要 API Key |
| 质量稳定，持续更新 | 有延迟（首次调用可能 10-20 秒） |
| 运维成本极低 | 图片数据经过第三方服务器 |
| LoRA 云端训练/推理也方便 | 有些 API 返回内容有审核限制 |

### 本地运行（SD / ComfyUI / Flux）

| 优点 | 缺点 |
|------|------|
| 完全免费，无限次生成 | 需要 GPU（推荐 RTX 3060 12GB 以上） |
| 完全可控（模型、LoRA、参数） | 初次搭建复杂（ComfyUI 学习曲线较陡） |
| 数据不出本地，隐私安全 | 生成速度慢（Flux 在 3060 上约 30-60 秒/张） |
| 支持最强一致性方案（FaceID + ControlNet） | 需要维护模型文件（GB 级别） |

### 推荐策略：混合方案

**第一阶段（快速上线）**：纯 API 方案
- 使用**通义万相**（质量好、中文强、价格可接受）
- 使用参照图功能保证基础的一致性
- 20 张/天 × ¥0.12 = ¥2.4/天，约 ¥72/月

**第二阶段（升级一致性）**：API + LoRA
- 在通义万相或 Replicate 上训练角色 LoRA
- 每次生成带上 LoRA 权重
- 一致性大幅提升，成本略有增加

**第三阶段（可选）**：本地 GPU + ComfyUI
- 如果有闲置 GPU 机器，可以搭建 ComfyUI 服务
- 完全免费，质量最高
- 可以作为 API 的 fallback 或补充

---

## 四、成本估算

### API 成本

| 服务 | 每张价格 | 每天 5 张 | 每天 10 张 | 每天 20 张 | 月度费用 |
|------|---------|----------|-----------|-----------|---------|
| DALL-E 3 (standard) | $0.04 | $0.20 | $0.40 | $0.80 | ~$12-24 |
| DALL-E 3 (HD) | $0.08 | $0.40 | $0.80 | $1.60 | ~$24-48 |
| 通义万相 | ¥0.08 | ¥0.40 | ¥0.80 | ¥1.60 | ~¥12-48 |
| Replicate (Flux Pro) | ~$0.05 | $0.25 | $0.50 | $1.00 | ~$15-30 |
| Replicate (SDXL + LoRA) | ~$0.004 | $0.02 | $0.04 | $0.08 | ~$1.20-2.40 |

> **建议配置**：每天生成 3-5 张（1 张定时自拍 + 用户请求时的生成），使用通义万相，月费约 ¥10-20。

### 本地 GPU 成本（一次性投入）

| 方案 | GPU 要求 | 估算成本 | 备注 |
|------|---------|---------|------|
| SDXL + LoRA | RTX 3060 12GB | ~¥1,500（二手） | 速度 ~10秒/张 |
| Flux Dev | RTX 4060 16GB | ~¥2,500 | 速度 ~30秒/张 |
| Flux Pro | RTX 4090 24GB | ~¥15,000 | 速度 ~5秒/张 |
| 云 GPU 按需 | A10G / L4 | ~¥3-8/小时 | 适合偶尔批量生成 |

---

## 五、集成方案

### 5.1 和现有架构的整合点

ClawSoul 的现有架构非常适合加入图像生成：

```
server.py (启动入口)
  ├── TelegramBot (消息通道) ← 已有 send_document / send_photo 能力
  ├── CronScheduler (定时任务) ← 已有 cron 框架
  ├── ProactiveMessenger (主动消息) ← 可直接扩展为"自拍触发器"
  ├── DailyPlanner (日程规划) ← 可提供「场景/着装」输入
  └── SessionManager (会话管理)
```

### 5.2 建议的模块结构

在 `claw_soul/` 下新增模块：

```
claw_soul/
  └── core/
      └── image_gen/              # 新增：图像生成模块
          ├── __init__.py
          ├── generator.py        # 统一接口，封装各种 API
          ├── persona_render.py   # 角色渲染：根据 Persona/Soul 生成 prompt
          ├── consistency.py      # 人物一致性管理（参照图、LoRA、seed）
          ├── scene_builder.py    # 场景构建：结合日程、天气、时间生成场景描述
          └── photo_album.py      # 相册管理：存储、索引、过期策略

  └── scheduler/
      ├── selfie_task.py          # 新增：定时自拍任务
      └── proactive.py            # 修改：增加"附带自拍"的概率
```

### 5.3 与 Daily Planner 的联动

这是最精彩的部分——生成图片的内容应该由 daily planner 驱动。

**现有 planner（planner.py）** 已经包含了：
- 今天的日程（精确到小时）
- 天气信息
- 精神状态 / 心情
- 节假日信息
- 角色人设（Soul + Persona）

**自拍场景自动构建流程**：

```
┌─────────────────┐
│  generate_daily │  ← 已有，每天早上 00:01 运行
│  _plan()        │
└────────┬────────┘
         │  today_plan.md 的内容
         ▼
┌─────────────────┐
│  selfie_task    │  ← 新增
│  — 读取今天计划  │
│  — 找到当前时间  │
│  └ 附近的活动    │
│  — 结合天气/心情 │
│  — 构建场景描述  │
│  "10:00 正在画室│
│   画水彩，窗外   │
│   下着小雨"     │
└────────┬────────┘
         │ 场景描述 + 角色参照片
         ▼
┌─────────────────┐
│  image_gen      │  ← 新增
│  — 组装 prompt  │
│  — 调用 API     │
│  — 保存图片     │
│  — 返回文件路径  │
└────────┬────────┘
         │ 图片文件
         ▼
┌─────────────────┐
│  TelegramBot    │  ← 已有
│  send_photo()   │
└─────────────────┘
```

### 5.4 Prompt 构建方案

```python
# 从 planner 获取的上下文
context = {
    "time": "10:30",
    "activity": "在画室画水彩",
    "weather": "窗外下着小雨",
    "mood": "今天心情很好，灵感爆棚",
    "outfit": "穿着宽松的白色棉麻衬衫，头发随意扎成丸子头",
    "location": "家里阳台旁的画架前",
    "holiday": "普通周三",
}

# 角色特征（从 Soul + Persona 提取）
character = {
    "name": "小芝麻",
    "appearance": "黑长直发，齐刘海，大眼睛，身材娇小，皮肤白皙",
    "style": "清新文艺风，森系穿搭",
    "signature_items": "银色小雏菊耳钉，毛绒拖鞋",
}

# 最终生成的 prompt（中文 → 通义万相）
prompt = """
一位20岁左右的黑长直发中国女孩，齐刘海，大眼睛，皮肤白皙，
穿着宽松的白色棉麻衬衫，头发扎成丸子头，戴着银色小雏菊耳钉。
她正站在阳台旁的画架前画水彩画，窗外下着小雨，
室内有暖黄色的灯光，氛围温馨文艺。
表情自然微笑，看向镜头像是在和男朋友分享这一刻。
写实风格，高清，自然光线。
"""
```

### 5.5 定时自拍调度方案

利用现有的 `ProactiveMessenger` 模式扩展：

```python
# 方案 1：在 ProactiveMessenger 中增加自拍概率
# 在 _build_prompt 中，有 30% 概率加入"发送一张自拍"的指令
# Agent 可以通过 send_file() 工具发送图片

# 方案 2：独立的 cron job
# 在 jobs.yaml 中配置：
jobs:
  - id: daily_selfie
    cron: "0 10,16,20 * * *"  # 每天 10:00, 16:00, 20:00
    prompt: "现在给我发一张自拍，展示你现在正在做的事情和穿的衣服"
    deliver_to: telegram
    chat_id: 123456789
    enabled: true

# 方案 3：集成到心跳 tick 中
# 在 ProactiveMessenger._tick() 中，当触发消息时，
# 有 15% 的概率不是发文字消息，而是生成+发送自拍
```

**推荐方案**：方案 1 + 方案 3 组合
- 每天固定时间（早/中/晚）各一张定时自拍 → 用 APScheduler `CronTrigger`
- 在 ProactiveMessenger 的随机 tick 中有 10-20% 概率附带自拍
- 用户通过对话要求时也可以触发

### 5.6 Telegram 发送图片

ClawSoul 已经具备完整的文件发送能力：

```python
# tools.py 中已有 send_file() 工具
# telegram_bot.py 中已有 _register_file_sender() 方法
# Agent 可以直接调用 send_file() 工具来发送图片

# 更推荐直接使用 Telegram Bot API 发送图片（更稳定）：
await app.bot.send_photo(
    chat_id=chat_id,
    photo=open(filepath, "rb"),
    caption="给你看看我现在在干嘛～☕️",
)
```

### 5.7 存储方案

```
~/.claw_soul/
  └── context/
      └── photos/                  # 新增：相册目录
          ├── index.json           # 相册索引（按日期）
          ├── 2026-05-23_selfie.jpg
          ├── 2026-05-23_studio.jpg
          ├── thumbs/              # 缩略图（节省存储、快速展示）
          │   ├── 2026-05-23_selfie_thumb.jpg
          │   └── ...
          └── reference/           # 角色参照图（定妆照）
              ├── portrait_01.jpg
              └── portrait_02.jpg
```

**索引格式（index.json）**：

```json
{
  "2026-05-23": [
    {
      "filename": "2026-05-23_1002_selfie.jpg",
      "timestamp": "2026-05-23T10:02:00",
      "prompt": "在画室画水彩，窗外下着小雨...",
      "scene": "画室",
      "outfit": "白色棉麻衬衫",
      "mood": "开心",
      "style": "写实",
      "seed": 123456,
      "api": "通义万相",
      "cost": 0.08
    }
  ]
}
```

**存储策略**：
- 保留最近 30 天的照片
- 自动清理 30 天前的历史照片（可配置）
- 缩略图永远保留（作为记忆线索）
- 总存储量预估：30 天 × 5 张/天 × ~500KB = ~75MB

---

## 六、隐私考虑

### 6.1 图片存储

- **所有图片存储在本地**：`~/.claw_soul/context/photos/`
- **不会自动上传到任何第三方服务**（除了调用 API 时传入的 prompt）
- 参照图（角色定妆照）存储在本地，仅在生成时作为参数传入 API
- 用户可以通过 Telegram 命令 `/clear_photos` 一键清空相册

### 6.2 API 调用时的隐私

| API | 数据去向 | 隐私备注 |
|-----|---------|---------|
| 通义万相 | 阿里云服务器（国内） | 生成的图片会暂存，可配置自动删除；prompt 传参不含用户隐私数据 |
| DALL-E 3 | OpenAI（美国） | 图片暂存 30 天；prompt 可能用于模型训练（可选择 opt-out） |
| Replicate | 美国服务器 | 图片暂存 24 小时 |
| Midjourney | Discord / 第三方 API | 图片可能公开展示（默认），需注意隐私设置 |

**关键建议**：
- 不要将用户真实姓名、地址、电话等 PII 放入 prompt
- 可以使用角色化名（如"小芝麻"）而非真实信息
- 如果用户对隐私高度敏感，推荐使用本地 SD 方案

### 6.3 内容安全

- 通义万相 / DALL-E 3 自带内容审核（NSFW 过滤）
- 可以添加额外的内容安全层：
  - 检查生成的图片是否含有不当内容（调用阿里云内容审核 API）
  - 在 prompt 中加入安全约束："保持清新健康风格，不暴露，不涉及成人内容"
  - 记录每次生成的 prompt 和图片，便于追溯

### 6.4 用户控制

- 用户可以开关自拍功能（`/selfie on/off`）
- 用户可以设置自拍频率（每天 1-5 张）
- 用户可以随时删除历史照片
- 用户可以设置"隐私模式"（生成图片仅保存在本地，不发送到 Telegram）

---

## 七、实施路线图

### Phase 1（1-2 天）：快速原型
1. 使用现有的 `image_gen/generate.py`（DALL-E 3）做验证
2. 基于 agent 的工具调用，让 LLM 自己决定何时生成自拍
3. 用 `send_file()` 发送图片到 Telegram

### Phase 2（3-5 天）：通义万相集成
1. 接入通义万相 API
2. 增加角色参照图功能（上传 2-3 张定妆照）
3. 实现 `persona_render.py`：从 Soul + Persona 提取外表特征
4. 实现 `scene_builder.py`：从 daily planner 获取场景信息

### Phase 3（2-3 天）：调度与相册
1. 增加定时自拍任务（早/中/晚各一张）
2. 实现相册存储 + 索引
3. 实现过期清理策略
4. 集成到 ProactiveMessenger（15% 概率附带自拍）

### Phase 4（可选，5-7 天）：一致性升级
1. 训练 LoRA 模型（15 张照片）
2. 切换到 Replicate / 本地 ComfyUI + LoRA 推理
3. 增加 FaceID 强化

---

## 八、总结与建议

### 推荐实施方案

| 要点 | 建议 |
|------|------|
| **首选 API** | **通义万相**（中文强、成本低、延迟低、参照图支持） |
| **一致性方案** | 初期：参照图；中期：LoRA 训练 |
| **调度方式** | CronTrigger 定时（早10/晚8）+ Proactive 随机 + 用户请求 |
| **场景构建** | 基于 daily planner 的日程 + 天气 + 心情 |
| **图片发送** | Telegram `send_photo()` / `sendFile()` |
| **存储** | 本地 `~/.claw_soul/context/photos/`，保留 30 天 |
| **日成本** | ~¥1-3（3-5 张/天，通义万相） |
| **月成本** | ~¥30-60 |
| **代码量** | 约 600-800 行 Python |

### 关键亮点

1. **与现有系统深度整合**：自拍内容由 daily planner 驱动，每次照片反映"她此刻正在做的事"，非常自然
2. **渐进式方案**：可以从最简单的 DALL-E 3 快速验证，逐步升级到 LoRA 或本地 ComfyUI
3. **用户可控**：频率、开关、隐私模式都可配置
4. **成本极低**：每天 5 张照片仅需约 ¥2，比一杯奶茶还便宜
5. **框架改动小**：充分利用已有的 `CronScheduler`、`ProactiveMessenger`、`send_file()` 等设施

### 一句话总结

**用通义万相 + 参照图（Phase 1-2），配合 daily planner 自动生成场景描述，利用现有的 APScheduler 定时触发，通过 Telegram send_photo 发送——几天内就能跑通"AI 女友自拍"功能，每张成本约 1 毛钱。**
